export interface User {
    _id: string
    username: string
    email: string
    assignedTasks: number
    role: string
    status: string
    createdAt: string
    updatedAt: string
    __v: number
}
